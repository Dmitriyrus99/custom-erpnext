# Traefik static configuration generated by Codex patch
entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

api:
  dashboard: true
  insecure: false

providers:
  docker:
    exposedByDefault: false

#@codex:middle-auth
http:
  middlewares:
    traefik-auth:
      basicAuth:
        users:
          - "root:$apr1$/uBAybm8$2x.U8umOSrGSjR7AHc5bz1"
    secure-headers:
      headers:
        stsSeconds: 15552000
        stsIncludeSubdomains: true
        stsPreload: true

#@codex:router-dashboard
  routers:
    traefik-dashboard:
      rule: "Host(`traefik.example.com`)"
      service: api@internal
      entryPoints: ["websecure"]
      middlewares: ["traefik-auth", "secure-headers"]
      tls: {}
