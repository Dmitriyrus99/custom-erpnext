import pytest


def test_company_uniqueness_and_rls():
	# Placeholder: create two companies, create invoices with same number/year per company
	# and assert allowed across companies but unique within a company once fields exist.
	# RLS behavior should be verified at DB-level if GUC erp.company_id is set.
	pass
