"""Top-level doctype package placeholder.

This package contains minimal placeholder directories so that Frappe's
FormMeta asset loader (which scans `apps/<app>/<app>/doctype/<name>/`)
finds the expected paths and does not raise FileNotFoundError during
desk view rendering. Business logic, JSON and client assets for these
doctypes live under `ferum_custom/ferum_custom/ferum_custom/doctype/`.
"""

